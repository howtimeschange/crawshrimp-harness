# 跨平台性能与上下文优化：最终审查及本地合并门禁

2026-09-18。工作分支 `codex/cross-platform-performance-20260918`；审查基线及合并前本地 main 均为 `715e24642700adb7863bb4441a16451a1214f4eb`。

用户最新授权：先提交当前已审查和自动回归通过的版本，再合并本地 main；未完成的实机验证继续单独记录，不作为已通过。没有推送、发布或部署授权。Windows ARM 原生发行链路不属于本轮。

## 审查范围

覆盖性能优化及相邻调用链：目录/PDF/图片预览与取消、懒加载/后台轮询/截图、事件投影与数据库保序、日志写入/归档、Office缓存与Windows路径、资源预算、启动与元数据缓存、运行时恢复、Windows运行时链路、中文可见输出、上下文/领域工具发现、技能及脚本调用、自动化权限和实际产物回读。

85个源码/测试/技能文件的最终哈希清单在本地检查点 `final-source-snapshot.json`。历史网页、商业资料、设计稿、根目录临时源码副本、生成的CLI闭包、模型文件、运行数据和密钥不进入提交。

## 发现及修复

| 编号 | 影响 | 修复与验证 |
|---|---|---|
| CR1 / P1 | AI视频目录读取仍引用已移出的函数，触发ReferenceError | 恢复导入；真实目录与实际IPC函数回归先失败后通过，相关12项通过 |
| CR2 / P1 | Windows多行文件写入成功后，默认LF→CRLF转换导致错误的字节不一致提示 | 写入显式禁用换行转换；保留原文件权限与共享冲突重试；模拟Windows换行先复现FILE_READBACK_MISMATCH，再验证成功 |
| CR3 / P2 | 快速离开并返回同一图片任务，旧轮询可能覆盖新状态、终止新轮询 | 给轮询加代次围栏，旧结果不写状态/计时器/在途标记；乱序回包测试先失败后通过 |
| CR4 / P2 | Office缓存目录扫描期间历史被删除，缓存错误导致新渲染失败 | 可选缓存I/O失败回退正常渲染；目录消失回归先失败后通过 |
| CR5 / P2 | 中文Office请求的首条可见进度出现英文 | 强化首条与每条可见进度的中文约束，最终真实模型复验待完成；未伪称模型输出100%可靠 |
| CR6 / P2 | Web“允许所有”未说明会话完全访问的实际范围 | 明确为“本会话完全访问（不再逐次审批）”；支持旧补丁升级，保留原权限语义；没有自动授予完全访问，最终UI读回待完成 |

跨套件检查还发现6项源代码结构断言停留在同步目录/静态导入/旧启动形式。逐项核对实际行为后迁移断言，其中返回图片工作台刷新配置改为执行真实生命周期回调；没有取消功能门禁。PDF预览已改为逐文件逐页缓存，不再整目录重置，因此旧重置重试断言对应的操作已不存在。

## 当前自动验证

证据目录：`artifacts/review-merge-20260918/`（本地证据，不纳入产品提交）。

- 客户端：`app-final.log`，816项通过。
- Python：`python-final.log`，1862项通过，1项跳过，53个子测试通过。保留现有依赖弃用警告。
- DSH集成：`integrations-full.log`，51项通过。
- 适配器/桌面合同：修正后完整1239项通过，见 `adapters-after-review.log`。
- 生产renderer构建：`renderer-build.log` 成功；标注组件仍作为较大的按需chunk，保留构建体积提示。
- 测试收集验证与 `git diff --check` 通过。
- 85项候选文件密钥形态扫描无命中；最终暂存后仍需复核文件范围与差异。

以上是本机自动验证，不能代替Windows物理x64、Intel Mac或实机稳定性结论。

## 实机门禁及本地Git状态

已有独立Astra轻度任务负责Mac/Windows最终版本验证。巴拉款号202426100106的既有抓虾脚本已经完成真实下载，JPEG/ZIP/Excel独立读回通过。通用网页操作与自然定时CSV场景已有实际成功证据；最终来源一致性、脚本固化重跑、剩余媒体/Office/恢复/长任务门禁按QA清单继续验收。

尚有明确阻碍：森马云盘网页搜索40106登录失效，等待用户恢复登录；Windows虚拟机输入对用户手动操作也异常，已按用户要求正常重启，正在同步最终版本，输入恢复与实际执行待确认。不能用缓存页面或后台API代替真实UI通过。

最终版本4小时连续稳定性必须由最终源码启动后计算，不能累加旧版本或重启分段。按用户最新指示，本次先显式暂存指定路径、复核暂存差异、创建本地commit并合并本地main。该Git交付不代表上述实机门禁已全部通过；最终commit与main指向以Git记录为准。

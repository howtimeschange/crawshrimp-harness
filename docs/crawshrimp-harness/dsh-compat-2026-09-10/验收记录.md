# DSH 选择性兼容实现与开发客户端验收

日期：2026-09-10。分支：`codex/dsh-selective-compat`。独立工作树：`/Users/xingyicheng/Documents/crawshrimp-harness-dsh-compat`。原仓库基线 `205500eead7bf72fa1a9b90df8287c41feed894a`。

## 交付范围

| 工作包 | 实现与结论 |
|---|---|
| C00 | compat 清单、关键定制基线 SHA-256、精确依赖版本校验、唯一锚点、幂等校验、暂存指纹补全与兼容报告 |
| C01 | 按新版源码回移植 MCP 重复游标检测；拒绝不完整工具集，保留上一代注册 |
| C02 | 按钮通过普通 Enter 的策略与输入状态机发送；忙时显示“加入队列／立即引导”，停止行为保留 |
| C03 | Markdown、代码高亮／复制、PDF 本地 worker、翻页、缩放、适宽和可选择文本；文件加载取消与过期结果隔离，保留 OfficePreview |
| C06 | HTML 静态文档及相对 CSS／图片／字体；空 sandbox、CSP、DOM 清理、CSS AST、目录约束与读取上限 |
| C09 | 真实模型长输出、流式上滚、断线恢复、本地图片通路走查；本次样本未复现必须修改旧 DSH 消息引擎的缺陷，不盲目回移植 |

不包含全量 DSH 升级、C04 附件进度、C05 多标签／双栏、C07/C08，不涉及数据库迁移。DSH 保持 0.1.2-rc.1，dsh-im 保持 4.11.0。没有 commit、push、发布或安装包替换。

## 实机环境与证据

macOS ARM64、Electron 43.1.0，开发渲染器 `127.0.0.1:5186`，独立后端 `19186`。测试数据与 Electron userData 都位于工作树 `.codex-tmp/compat-qa`，原用户数据目录不变。

早期使用过本地固定回复，用户纠正后已停止模拟服务，恢复 `https://api.deepseek.com` 和已有配置中的真实 Key。以下模型结果来自真实 DeepSeek Flash 调用，固定回复只用于早期流程探测，不作真实模型验收证据。验收文件本身是明确构造的测试样本，不声称由模型生成。

- 普通 Enter 和发送按钮均产生真实回复：`137 × 29 = 3973`。
- 运行中输入第二条消息，按钮显示“加入队列”；100 条长输出完成后继续处理排队消息，出现 `LONG_REAL_DONE` 和 `QUEUE_REAL_OK`。
- 第二次长输出 120 条文件管理建议时上滚；内容高度 `3349 → 3669`，滚动位置 `637 → 637`，见 `stream-scroll.json`。
- 对真实 Electron 页使用 CDP 网络离线 2 秒再恢复；恢复前后会话文本长度均为 4776，长回复和排队结果仍在；静止上滚位置 `737 → 737`。这是客户端网络故障注入，不是关闭模型服务。
- 本地图片经编辑器粘贴、原生草稿、真实发送进入模型：模型识别出 `LOCAL IMAGE 480 x 180`。回看消息流仅有一张对应图片，naturalWidth=480。
- Markdown `.markdown` 完整扩展名、表格、粗体、代码块、本地相对图片显示；`R$ 123.45` 和 `$29.99` 保持文本，原始 `<script>` 被作为文本显示。
- HTML 标题蓝色、段落红色、相对 CSS 背景图与 `<img>` 均正确显示。恶意脚本／iframe／表单被移除，外网属性为 0；相对越界、编码越界、绝对路径与外部 URL 被拒绝。无认证调用新资源端点返回 401。
- PDF 实际翻页 1→2、缩放到125%、文本层内容可选择；无效 PDF 有错误说明。发现并修复“加载提示改变容器高度触发反复重绘”：ResizeObserver 只在宽度变化时重绘。
- 代码高亮与复制／已复制反馈通过，长文本仅读取前256 KB，UTF-8 尾部未出现拆字替代符。PDF 和复制按钮已按用户反馈补全样式、图标、悬停、禁用、焦点及深浅主题。

图片证据：`html.png`、`md.png`、`pdf-light.png`、`pdf-dark.png`、`code-light.png`、`code-dark.png`。深浅样式走查临时切换 DOM 主题后恢复，不改变持久化主题偏好。

## 自动化与构建

- C01/C02：9 项通过，含重复游标两种循环、正常多页、取消保留旧工具、锚点变更失败与幂等、queue/steer 分发。
- 文本预览：3 项通过，覆盖完整扩展名、字节上限、UTF-8 与失败状态。
- Python 相对资源／会话资源：15 项通过，含目录穿越、URL 编码、符号链接越界、类型白名单、单资源上限。
- Web RPC／会话恢复／附件适配／资源面板相邻回归：49 项通过。
- App 全套：778 项，773 通过、5 个既有失败。原分支同一批相关测试已复跑，失败原先存在；此次补齐资源面板的异步与依赖模拟，原有10个失败减少到5个。
- 新增依赖最终锁定 markdown-it 14.3.1、DOMPurify 3.4.15、highlight.js 11.11.1、PDF.js 5.4.149、css-tree 3.1.0；npm audit 未报告这五项的已知漏洞。
- Vite 构建通过。文档组件按需加载；PDF worker/CMap/字体/WASM/许可证全部打入本地产物。
- `stage-runtime.mjs --force` 全新安装、补丁应用、内置技能构建、Web profile 配置检查通过；新兼容补丁均 changed=true / verified=true，见 `compat-report.json`。

尚存在的基线失败：apiConnection 的 VM 未注入 process；artifactSessionCompatibility 的旧请求序列断言；migrationUiParity 两个 DOM mock 缺少 setAttribute；themeVisualContracts 的旧设置 CSS 正则。没有将这些失败标为通过，也没有在本次范围外改动其产品实现。

## 边界与后续复查条件

HTML/Markdown 静态预览禁用脚本、外网资源和链接跳转；CSS @import、表单、iframe、内联 SVG、音视频不参与静态展示。单资源8 MB、最多64项／24 MB；文本256 KB，高亮64 KB；加密 PDF 交给系统阅读器。

C09 的本次证明范围是实际测试会话（7轮、包括100/120条长输出）与一次客户端离线恢复，不能外推为千轮历史、所有嵌套工具图片或所有网络故障均无问题。未更改现有重连／滚动引擎。若后续出现可复现样本，应独立登记并补回归测试后修复。

现有 Office、IM、审批和自动化定制入口保持，相关历史补丁仍由原编排应用；本轮没有重新执行所有 IM 渠道和 Office 页级生成任务。没有 Windows、Intel Mac 或打包安装验收。


## 后续反馈修复：Markdown 主题与资源交互

- Markdown 的沙箱文档通过宿主 iframe 的 color-scheme 跟随应用主题，无需脚本、重新读取资源或重载文档。实机深色背景为 rgb(28,28,36)，浅色为 rgb(255,255,255)，文字、表格、代码块同步变色；保留图片本身的颜色。
- 修复紧凑卡片原先“悬停色与背景色相同”的问题。文件整行、区域标题、搜索／添加／收起／更多菜单均有独立反馈；整行背景通过伪元素 opacity 120ms 渐显，按钮160ms轻微按压，折叠箭头提示状态。行文字不随悬停移动。
- fine-pointer/hover 条件限制鼠标效果；键盘提供即时 focus-visible 轮廓，prefers-reduced-motion 取消缩放与箭头过渡。
- 真实客户端复查 hover opacity 0→1、深浅主题、键盘 Tab/Shift+Tab 焦点和减少动效模式；补充定向21项测试与Vite构建通过。
- 证据：md-dark.png、md-light.png、resource-hover.png、theme-hover.json。

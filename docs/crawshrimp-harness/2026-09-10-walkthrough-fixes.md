# 全功能走查问题修复与回归记录

2026-09-10。本地开发客户端 v0.1.13。对应 [原走查报告](2026-09-10-full-feature-experience-walkthrough.md)。

G01–G06 已修复并完成相关回归。G07（tldraw 许可提示）按用户本轮明确答复“保持现状”，未修改，也不标记为已解决。

## 复现、修复与证据

| 问题 | 本轮复现 | 修改 | 验证 |
|---|---|---|---|
| G01 导航边界错位 | 真实 Electron 折叠导航后 Cmd+R，首次打开生图：约54px导航旁露出聊天正文，工作台从约270px开始 | nav-click 使用当前实际导航宽度，移除历史最大宽度覆盖 | 折叠刷新后，生图与任务中心均贴齐导航；展开与再次切换生图也贴齐。截图在本任务 CUA 记录中 |
| G02 空 Prompt 虚假运行态 | 新建“AI 生图任务 3”，空 Prompt 开始：暴露 IPC 英文错误，删除被“生成中”禁用；数据库随后为 running | 提交前 trim 校验；草稿自动保存不再写入执行状态；IPC 异常读取服务端任务状态，查询失败继续轮询核实 | 空白/纯空格均中文阻止；编辑名称、自动保存和 Cmd+R 后任务仍 draft、无新生成结果、删除可用。测试覆盖 IPC 失败回读与自动保存不写 running |
| G03 跨运行产物混合 | 原 run14 stopped/0结果实例仍列出 run12–13 三个旧文件；本轮 run15 又累加第4个文件 | 任务实例顶部和输出面板统一按 run_id 读取 task_runs.output_files；空数组为本次零输出；顶部标注运行编号 | run16/17 各仅本次ZIP+Excel；run18 stopped、0记录、output_files=[]，客户端无旧文件；run19 再次成功后仅本次ZIP+Excel |
| G04 继承旧进度/日志 | 修复前回归测试确认新 running 合并后仍带旧款号与100%；日志接口保留所有历史运行段 | 新运行立即清空日志/输出/进度；跨run快照替换而非合并；初始化异步读回通过版本号防过期写入；日志只展示最新“新运行”段；输出页最小化摘要显示文件概况 | 改参数启动、成功后再次启动、停止后重跑均在首帧显示准备中/0%/空输出；run19日志只含12:57:36新运行及本次两款输入，无前轮完成日志 |
| G05 下载反馈 | 旧下载操作打开原生目录框，返回后无可见进度/完成提示，单图下载仍可点 | 下载前锁定任务UID与文件清单；逐张下载反馈数量；批量/单张按钮禁用；完成提示目录；失败逐项列出并可重试 | 真实选择两张图片，显示“已完成0/2”、按钮disabled，最后“已下载2/2张”与目录；两PNG完整解码、2880×2880。模拟第二项失败和重复点击测试通过 |
| G06 零匹配摘要 | run15无匹配自然完成，原界面仅显示1条记录 | 从本次Excel真实下载状态计算已下载、未匹配款号、失败数；全/部分未匹配显示业务摘要，使用提醒色 | run15显示下载0张/未匹配1个；run16显示下载2张/未匹配0个；run19显示下载2张/未匹配1个，数据库3条结果与业务统计区分清楚 |
| G07 tldraw提示 | 沿用原报告证据 | 用户要求保持现状 | 未关闭 |

## 本轮业务执行

使用原开发数据目录 `/private/tmp/crawshrimp-harness-main-dev-20260907/runtime-data`，前端5173、核心18768、DSH19069。沿用报告测试目录和款号，没有发送IM消息或新增自动化。

| run | 场景 | 实际结果 |
|---|---|---|
| 15 | 修复前无匹配复现 | done，1条未匹配结果，1个Excel |
| 16 | 修复后正常款号 | done，2张下载，ZIP+Excel |
| 17 | 成功后重复启动 | done，2张下载，独立ZIP+Excel；点击停止前已自然完成，不计停止通过 |
| 18 | 即时停止 | stopped，0记录、无输出；客户端空结果 |
| 19 | 停止后混合正常/无匹配重跑 | done，3条记录：下载2张，未匹配1个款号；ZIP+Excel |

本轮复现导致任务 `9ef61bca9d6d47d3810ebada8519ead2` 被旧自动保存持久化为 running。确认其空Prompt、没有生成run后，通过本地API单独恢复为draft，未删除记录。修复后新测试任务 `caeffe9b03b74366a44d2933e40685c2` 同样为draft。

图片保存于 `/private/tmp/crawshrimp-walkthrough-fix/download-after`：6,970,676及5,718,031 bytes，均完整解码2880×2880。完整SHA、测试任务状态和本轮Excel内容见 [只读证据](qa-2026-09-10-fixes/evidence.json)。可用 [采集脚本](qa-2026-09-10-fixes/collect_evidence.py) 重读。

## 自动验证与交付边界

- 相关 Node 测试通过；命令和最终数量见 [测试输出](qa-2026-09-10-fixes/tests.log)。新增测试先确认空Prompt/旧进度失败，再验证修复后通过。
- `npm run vite:build --prefix app` 通过；仅有既有大bundle警告。
- `git diff --check` 通过。
- 真实UI证据由本任务 CUA 截图/AX记录提供，未另存截图附件。新生成请求失败与下载部分失败由模拟回归覆盖，没有向生图服务商新增付费生成。
- 这是macOS开发客户端验收；本轮未重新打DMG、未做安装包/Windows/Intel/冷启动验收，未覆盖原报告列出的其他缺凭据业务。
- 保留进入任务前 App.vue、AgentWebView.vue、SessionResources.vue 及 slots client 中已有的修改。本轮未提交Git、未推送、未发布。

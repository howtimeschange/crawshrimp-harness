## v1.4.36

### 更新亮点

- macOS 桌面构建接入 `Developer ID Application: yicheng xing (62AR7GLNK3)` 签名和 Apple notarization，GitHub Actions 会使用 App Store Connect API Key 准备公证凭据。
- macOS 打包开启 Hardened Runtime，并补充 Electron/Python 打包所需 entitlements，降低 Gatekeeper 拦截和运行时签名异常风险。
- CI 增加 `APPLE_API_*`、`MAC_CSC_*` secrets 注入，并通过 `CSC_NAME` 锁定 Developer ID Application 身份。
- 新增桌面 macOS 签名/公证配置回归测试，避免后续 workflow 或 build config 误删发行链路。

### 验证

- 已校验 entitlements plist：`plutil -lint app/assets/entitlements.mac.plist app/assets/entitlements.mac.inherit.plist`。
- 已校验构建 YAML 可解析：`app/build.yml`、`.github/workflows/build-desktop.yml`。
- 已运行签名/公证配置测试：`node --test tests/desktop-macos-signing-notarization.test.js tests/workflow-triggers.test.js tests/desktop-auto-update-disabled.test.js`。
- 已运行 app 前端测试：`npm test`。
- 已用 App Store Connect API Key 验证 notarytool 鉴权：`xcrun notarytool history`。

### 产物命名

- macOS Apple Silicon：`crawshrimp-v1.4.36-mac-arm64.dmg`
- macOS Intel：`crawshrimp-v1.4.36-mac-x64.dmg`
- Windows x64：`crawshrimp-v1.4.36-win-x64.exe`
- 自动更新元数据：已关闭；macOS 签名 / 公证链路已接入 CI

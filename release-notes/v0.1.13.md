## v0.1.13

- 修复 macOS Intel 版首次打开智能体页面时，后台端口切换与 renderer 首次请求并发导致的 `Failed to fetch`、页面停在启动画面和核心状态红绿抖动。
- 启动状态查询现在等待桌面服务就绪；本地 API、SSE 事件流和媒体签名请求在首次使用前统一同步当前端口与 token，避免依赖多次刷新恢复。
- 增加并发首次连接、服务恢复和 preload 连接契约回归测试，覆盖 macOS arm64/x64 共用的启动路径。

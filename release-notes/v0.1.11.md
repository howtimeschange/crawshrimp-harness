## v0.1.11

- 增强 IM 自然语言模型控制，支持列出当前可用模型、切换会话模型，并补齐抓虾、GPT-5、Claude、Gemini、DeepSeek 等常用别名识别。
- 修复 IM 权限模式指令路由，让“去掉审批”“开启完整访问”等表达直接更新 DSH 权限模式，避免先进入无关探索步骤。
- 完善跨通道文本桥接的审批摘要与确认路径，覆盖 WeChat、DingTalk、Feishu、QQ、WeCom 共享接入层。
- 优化 DSH 注入侧边栏展开/收起图标，并新增回归测试锁定视觉状态。

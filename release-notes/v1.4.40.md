## v1.4.40

### 更新亮点

- 天猫运营助手「天猫包装图片上传」头图取图规则收紧：1:1 主图固定选取“主图”前 2 张 + “微详情”前 2 张。
- 3:4 主图固定选取“主图”前 2 张 + “微详情”前 3 张，商品竖图继续从主图包中选取 1 张 `1440_2160(天猫)`。
- 新规则作用在云盘素材分类阶段，上传到淘宝图片中心、写入 `mainImagesGroup` / `threeToFourImages` / `guideImageGroup.verticalImage` 的流程保持不变。
- 新详情和旧详情发布链路共用同一套头图组件写入逻辑，因此本次头图选择规则对两种详情链路都生效。
- 新增回归测试覆盖多张同尺寸素材时只保留序号靠前的 1:1、3:4 和商品竖图，避免主图候选过多挤占微详情素材。

### 验证

- 已运行桌面应用测试：`npm --prefix app test`。
- 已运行仓库级适配器 JavaScript 测试：`node --test tests/*.test.js`。
- 已运行后端 Python 单测：`PYTHONPATH=. python3 -m unittest discover -s tests -p 'test_*.py' -v`。
- 已校验天猫包装图片上传脚本语法：`node --check adapters/tmall-ops-assistant/tmall-packaging-upload.js`。
- 已运行天猫包装图片上传 JavaScript 回归测试：`node --test tests/tmall-ops-assistant-packaging-upload.test.js`。
- 已运行 Python 编译检查：`python3 -m py_compile core/js_runner.py core/api_server.py`。
- 已运行 Git whitespace 校验：`git diff --check`。

### 产物命名

- macOS Apple Silicon：`crawshrimp-v1.4.40-mac-arm64.dmg`
- macOS Intel：`crawshrimp-v1.4.40-mac-x64.dmg`
- Windows x64：`crawshrimp-v1.4.40-win-x64.exe`
- 自动更新元数据：已关闭；macOS 签名 / 公证链路沿用 `v1.4.36` 起接入的 Developer ID notarization 流程

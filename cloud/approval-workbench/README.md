# 已弃用：云端审批与任务机

此目录是上游抓虾留下的历史实现，不属于个人本地智能体 Harness 的产品能力，不应再为 Harness 部署或接入。

Harness 已移除设置与嵌入页面，禁用机器自动启动，并让旧注册、启动、批次同步、云提示词 API 返回 HTTP 410。旧配置和数据库记录保留用于历史追溯，但不参与运行。个人账号使用独立 Supabase Auth，不向云端开放本机任务执行权限。

本次仓库弃用不关闭或删除已有远程 Worker、数据库和 R2 资源；它们可能仍被上游抓虾使用。也不影响本地 DSH 审批、Adapter 运行和本地自动化计划。
